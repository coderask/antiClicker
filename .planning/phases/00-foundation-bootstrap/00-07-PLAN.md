---
phase: 00-foundation-bootstrap
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md
autonomous: false
requirements:
  - FND-03
tags:
  - manual
  - external-service
  - google-cloud
user_setup:
  - service: google-cloud-console
    why: "Phase 4 will need a Maps JavaScript API key. Setting the quota cap + budget alerts now (rather than later) closes Pitfall 5 from research/PITFALLS.md before a leaked key can cause unbounded billing."
    env_vars: []
    dashboard_config:
      - task: "Enable the Maps JavaScript API in a Google Cloud project"
        location: "Google Cloud Console → APIs & Services → Library → Maps JavaScript API → Enable"
      - task: "Set a Queries Per Day (QPD) quota cap on the Maps JavaScript API"
        location: "Google Cloud Console → APIs & Services → Quotas → Maps JavaScript API → Edit (set ~1000 req/day for personal use)"
      - task: "Set up budget alerts at $5, $20, $50 thresholds"
        location: "Google Cloud Console → Billing → Budgets & alerts → Create budget"
      - task: "Create an API key (do NOT paste it into electron-store yet — that's Phase 4)"
        location: "Google Cloud Console → APIs & Services → Credentials → Create credentials → API key"
      - task: "Restrict the API key to HTTP-Referrer http://localhost/* (Phase 0's HTTP server runs on http://127.0.0.1:<ephemeral> which matches localhost referrer policy per Pitfall 4)"
        location: "Google Cloud Console → Credentials → click the new key → Application restrictions → HTTP referrers → add http://localhost/* and http://127.0.0.1/*"
must_haves:
  truths:
    - "The user (manually) enabled Maps JavaScript API on a Cloud project"
    - "The user (manually) set a QPD quota cap"
    - "The user (manually) configured budget alerts at $5/$20/$50"
    - "The user (manually) created an API key, restricted to localhost referrers, and stored it OUT of the repo (Phase 4 will paste it into electron-store)"
    - "A checklist artifact at .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md captures the confirmation"
  artifacts:
    - path: ".planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md"
      provides: "Permanent record of the manual Cloud Console steps + user confirmation"
  key_links:
    - from: "CLOUD-CONSOLE-CHECKLIST.md"
      to: "research/PITFALLS.md Pitfall 5 (billing surprise) and Pitfall 4 (referrer restriction)"
      via: "checklist references both pitfalls and confirms mitigation"
      pattern: "Pitfall 5|Pitfall 4|PITFALLS"
---

<objective>
Close Pitfall 5 from `.planning/research/PITFALLS.md` ("Google Maps billing surprise from satellite tile usage + unrestricted key") at the start of Phase 0 rather than letting it linger until Phase 4. This is an external-service configuration step that Claude cannot automate — the user must click through Google Cloud Console — but it's a one-time, ~5-minute task whose cost of NOT doing it (an accidentally-leaked unrestricted key racking up surprise charges) is much larger than the cost of doing it now.

Purpose: De-risk Phase 4 by making sure the Maps API key never exists in an unrestricted form. No code is written here; this is a pure human-action plan.

Output: A single checklist artifact recording that the user completed the four Cloud Console steps. The actual API key is NEVER committed to the repo — Phase 4 will paste it into electron-store at runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/research/PITFALLS.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-VALIDATION.md

Why this is plan 00-07 and autonomous=false:
- Pitfall 5 in PITFALLS.md is flagged as severity MAJOR ($-impact, security)
- 00-RESEARCH.md Pitfall 8 explicitly says: "The plan should have a non-code checklist task like: User confirms in Google Cloud Console: Maps JavaScript API enabled, QPD quota cap set, budget alert at $5/$20/$50. This is NOT a code task; mark it as a manual verification step in the plan."
- 00-VALIDATION.md "Manual-Only Verifications" table lists exactly this step
- Wave 1 means it can run in parallel with the scaffold (00-01); doesn't block anything technical
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7.1: User completes Google Cloud Console setup (4 steps)</name>
  <files>.planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/research/PITFALLS.md (Pitfall 5 in full — the threat model + how the mitigations close it)
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pitfall 8 — explicit guidance that this is a manual checklist step)
  </read_first>
  <what-built>
    The executor has produced no code. This task only verifies the human did four external-service configurations:
    1. Enabled Maps JavaScript API on a Cloud project
    2. Set a QPD quota cap (~1000 req/day for personal use)
    3. Set budget alerts at $5 / $20 / $50
    4. Created an API key restricted to http://localhost/* and http://127.0.0.1/* HTTP referrers
  </what-built>
  <how-to-verify>
    1. Visit https://console.cloud.google.com/apis/library/maps-backend.googleapis.com (or search "Maps JavaScript API" in the Cloud Console library) and confirm it is enabled on a project you own.
    2. Visit Quotas page for the Maps JavaScript API; confirm a per-day quota cap is set (e.g., 1000 requests/day).
    3. Visit Billing → Budgets & alerts; confirm at least one budget exists with alert thresholds at $5, $20, $50 (or equivalents).
    4. Visit Credentials; confirm the API key has "Application restrictions: HTTP referrers" with `http://localhost/*` and `http://127.0.0.1/*` in the allowed list. Confirm the actual key value is NOT pasted anywhere in the repository (it will be entered into electron-store at runtime in Phase 4).
    5. After confirming all four, write `.planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` with the literal sections below.
  </how-to-verify>
  <action>
    The executor writes `.planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` with the following structure ONLY AFTER the user confirms in the chat that all four steps are done. The file content:

    Heading: `# Phase 0 — Google Cloud Console Setup Checklist`
    Date line: `Completed: <ISO date confirmed by user>`
    Section "Steps Completed" with a checked-box markdown list of all 4 steps verbatim from the dashboard_config in the frontmatter user_setup.
    Section "Mitigations Closed" referencing Pitfall 4 (HTTP-referrer restriction works because localhost is whitelisted) and Pitfall 5 (billing capped by QPD + budget alerts).
    Section "API Key Storage" stating literally: "The API key value is NOT stored in this repository. It will be entered at runtime via the Phase 4 settings UI, which persists it via electron-store to the OS user-data dir."
    Section "Confirmation" with the user's name (if they offer it) or just "User confirmed all four steps" plus a date.

    No actual key value goes into the checklist file. If the user attempts to paste the key into the chat, the executor MUST refuse and remind them keys live only in electron-store at runtime.
  </action>
  <resume-signal>Type "confirmed" once all four Cloud Console steps are complete, OR type "skip" if you want to defer to Phase 4 (with the acknowledgment that an unrestricted key is a billing risk).</resume-signal>
  <acceptance_criteria>
    - User has explicitly typed "confirmed" or equivalent confirmation in the chat
    - File `.planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` exists
    - `grep -q "Maps JavaScript API" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds
    - `grep -q "QPD" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` OR `grep -q "quota" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds
    - `grep -q "budget" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` (case-insensitive ok) succeeds
    - `grep -q "localhost" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds (referrer restriction noted)
    - `grep -q "Pitfall" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds (mitigations referenced)
    - `! grep -E "AIza[A-Za-z0-9_-]{30,}" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds (no actual API key leaked into the artifact — Google Maps API keys start with `AIza`)
    - File is committed to git
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md && grep -q "Maps JavaScript API" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md && grep -qi "budget" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md && grep -q "localhost" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md && ! grep -E "AIza[A-Za-z0-9_-]{30,}" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md && echo ok</automated>
  </verify>
  <done>Checklist artifact exists with the four steps confirmed, mitigations referenced, and zero API key material committed to the repo.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds
- `! grep -E "AIza[A-Za-z0-9_-]{30,}" .planning/phases/00-foundation-bootstrap/CLOUD-CONSOLE-CHECKLIST.md` succeeds (no leaked key — security invariant)
- Pitfall 5 (billing surprise) and Pitfall 4 (referrer restriction) are referenced in the checklist
</verification>

<success_criteria>
- The user has, by their own admission, completed the four external Cloud Console steps
- An artifact records that fact in version control (for future-self memory)
- The actual API key value is NOT in the repository
- Phase 4 can pick up here and add the runtime-key-entry UI without re-doing this manual setup
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-bootstrap/00-07-SUMMARY.md` documenting:
- Confirmation that the four Cloud Console steps were completed (paraphrase the checklist, do not duplicate)
- Note that the key value is NOT in the repo and will be entered at runtime in Phase 4 via electron-store
- Reference to Pitfall 4 and Pitfall 5 being mitigated by this manual step
</output>
