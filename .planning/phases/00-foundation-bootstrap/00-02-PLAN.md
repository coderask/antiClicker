---
phase: 00-foundation-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - src/main/index.ts
  - src/main/ipc.ts
  - src/shared/types.ts
  - src/shared/ipc-channels.ts
autonomous: true
requirements:
  - FND-01
tags:
  - electron
  - main-process
  - security
must_haves:
  truths:
    - "src/main/index.ts constructs BrowserWindow with contextIsolation:true, nodeIntegration:false, sandbox:true"
    - "Main process registers ipcMain.handle('ping') returning the string 'pong'"
    - "Main process imports startRendererServer from ./renderer-server.js (the file is created in plan 00-03) and uses its return value as the BrowserWindow loadURL target in packaged mode"
    - "Main process imports initConfigStore from ./config-store.js (created in plan 00-04) and invokes it inside app.whenReady"
  artifacts:
    - path: "src/main/index.ts"
      provides: "Electron app entry point: app lifecycle, BrowserWindow with secure webPreferences, dev/packaged URL switch, IPC bootstrap"
      contains: "sandbox: true"
    - path: "src/main/ipc.ts"
      provides: "ipcMain handler registration (Phase 0: ping + config:get-launch-count)"
    - path: "src/shared/types.ts"
      provides: "Shared TypeScript types (AppConfig type re-exported, IPC payload types)"
    - path: "src/shared/ipc-channels.ts"
      provides: "String constants for IPC channel names (ping, config:get-launch-count)"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/renderer-server.ts (provided by plan 00-03)"
      via: "import { startRendererServer } from './renderer-server.js'"
      pattern: "startRendererServer"
    - from: "src/main/index.ts"
      to: "src/main/config-store.ts (provided by plan 00-04)"
      via: "import { initConfigStore } from './config-store.js'"
      pattern: "initConfigStore"
    - from: "src/main/index.ts"
      to: "src/main/ipc.ts"
      via: "import { registerIpc } from './ipc.js'"
      pattern: "registerIpc"
---

<objective>
Write the Electron main-process entry point with the FND-01 security invariants baked in: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true`. Wire the dev/packaged URL switch (electron-vite's `ELECTRON_RENDERER_URL` in dev; HTTP server in production — server itself lands in 00-03). Register the IPC bootstrap (`registerIpc()`) and the config store init (`initConfigStore()` — wrapper lands in 00-04). Establish the shared types and IPC channel constants files that preload (00-05) will consume.

Purpose: This plan is the load-bearing security boundary for the entire app. Every future feature flows through these webPreferences. Getting them right (and greppably explicit) is FND-01's job.

Output: `src/main/index.ts` + `src/main/ipc.ts` + shared types/channels — the spine that 00-03 (HTTP server), 00-04 (config store), and 00-05 (preload) plug into.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-01-SUMMARY.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/research/PITFALLS.md

<interfaces>
<!-- Forward declarations: 00-02 imports symbols that 00-03 and 00-04 implement in the same wave. -->
<!-- These are the contracts. Implementations must match. -->

From plan 00-03 (src/main/renderer-server.ts):
```typescript
export async function startRendererServer(): Promise<string>;  // returns "http://127.0.0.1:<port>/"
export async function stopRendererServer(): Promise<void>;
```

From plan 00-04 (src/main/config-store.ts):
```typescript
export function initConfigStore(): Store<AppConfig>;
export function getStore(): Store<AppConfig>;
export type AppConfig = { launchCount: number; googleMapsApiKey: string | null };
```

From @electron-toolkit/utils (already installed by 00-01):
```typescript
export const is: { dev: boolean };
```

Wave coordination: 00-02 / 00-03 / 00-04 are siblings in wave 2. Each writes against the others' published contracts. After all three land, `npm run build` (which compiles main) succeeds.
</interfaces>

<pitfalls>
- PITFALL 1 from 00-RESEARCH.md (Pitfall 1): scaffold ships sandbox:false. We MUST set sandbox:true explicitly.
- PITFALL 4 from 00-RESEARCH.md (Pitfall 7): never call mainWindow.loadFile(). Always loadURL(rendererUrl).
- The `import.meta.url` + `fileURLToPath(dirname(...))` pattern is mandatory under ESM — there is no `__dirname` global.
- File extensions in imports MUST be `.js` (not `.ts`) per ESM convention even though source is `.ts` — electron-vite emits `.js` and the runtime resolves them.
</pitfalls>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Write src/shared/types.ts and src/shared/ipc-channels.ts</name>
  <files>src/shared/types.ts, src/shared/ipc-channels.ts</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 4 for AppConfig shape; Pattern 5 for the preload api surface that imports these types)
  </read_first>
  <action>
    Create src/shared/ipc-channels.ts exporting a frozen object `IpcChannels` with two string-literal members: `Ping: 'ping'` and `ConfigGetLaunchCount: 'config:get-launch-count'`. Use `as const` so values are string literals at the type level. Export a derived `IpcChannel` type as `typeof IpcChannels[keyof typeof IpcChannels]`. Create src/shared/types.ts re-exporting `AppConfig` from `'../main/config-store.js'` (forward reference — main/config-store.ts is built by plan 00-04 in the same wave). Also define and export `PingResponse = 'pong'` and `LaunchCount = number` as named types. These shared files are imported by preload (00-05) and main IPC handlers (this plan). Use TypeScript `import type` syntax where importing only types to keep the runtime bundle clean.
  </action>
  <verify>
    <automated>test -f src/shared/types.ts && test -f src/shared/ipc-channels.ts && grep -q "IpcChannels" src/shared/ipc-channels.ts && grep -q "'ping'" src/shared/ipc-channels.ts && grep -q "'config:get-launch-count'" src/shared/ipc-channels.ts && grep -q "PingResponse" src/shared/types.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - File `src/shared/ipc-channels.ts` exists and `grep -q "as const" src/shared/ipc-channels.ts` succeeds
    - `grep -q "Ping:" src/shared/ipc-channels.ts` and `grep -q "ConfigGetLaunchCount:" src/shared/ipc-channels.ts` both succeed
    - File `src/shared/types.ts` exists and exports `PingResponse` (`grep -q "export type PingResponse" src/shared/types.ts` succeeds)
    - File `src/shared/types.ts` re-exports `AppConfig` (`grep -q "AppConfig" src/shared/types.ts` succeeds)
    - `npm run typecheck` does NOT yet pass (config-store.ts is not written until 00-04), but the file compiles in isolation when AppConfig is imported as `import type`
  </acceptance_criteria>
  <done>Shared types and channel constants exist; preload (00-05), main IPC (this plan task 2.3), and renderer can all import from `@shared/types` and `@shared/ipc-channels`.</done>
</task>

<task type="auto">
  <name>Task 2.2: Write src/main/index.ts with secure webPreferences and dev/packaged URL switch</name>
  <files>src/main/index.ts</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 2 — the canonical secure-defaults BrowserWindow; Anti-Patterns to Avoid section; Pitfall 1 + Pitfall 7)
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/research/PITFALLS.md (Pitfall 4 — referrer restrictions reinforce why we never loadFile)
  </read_first>
  <action>
    Write src/main/index.ts as an ESM module. Imports: app, BrowserWindow from 'electron'; fileURLToPath from 'node:url'; dirname, join from 'node:path'; is from '@electron-toolkit/utils'; startRendererServer, stopRendererServer from './renderer-server.js' (the file lands in 00-03 — this is a forward import); initConfigStore from './config-store.js' (lands in 00-04); registerIpc from './ipc.js' (Task 2.3 of this plan). Resolve __dirname via `const __dirname = dirname(fileURLToPath(import.meta.url))`. Define async function createWindow() that: (a) computes rendererUrl: if `is.dev && process.env.ELECTRON_RENDERER_URL` is truthy use it directly, otherwise `await startRendererServer()`; (b) constructs `new BrowserWindow({ width: 1200, height: 800, show: false, webPreferences: { preload: join(__dirname, '../preload/index.mjs'), contextIsolation: true, nodeIntegration: false, sandbox: true, webSecurity: true } })` — the four security flags must appear LITERALLY in the source so they are greppable; (c) calls mainWindow.on('ready-to-show', () => mainWindow.show()); (d) awaits mainWindow.loadURL(rendererUrl). Wire app.whenReady().then(async () => { initConfigStore(); registerIpc(); await createWindow(); app.on('activate', () => { if (BrowserWindow.getAllWindows().length === 0) void createWindow(); }); }). Wire app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit(); }). Wire app.on('before-quit', async () => { await stopRendererServer(); }) for Pitfall 6 cleanup. Add app.on('web-contents-created', (_e, contents) => { contents.setWindowOpenHandler(() => ({ action: 'deny' })); }) as defense-in-depth against window.open from the renderer. NEVER call mainWindow.loadFile() anywhere — verify by grep at end.
  </action>
  <verify>
    <automated>grep -q "contextIsolation: true" src/main/index.ts && grep -q "nodeIntegration: false" src/main/index.ts && grep -q "sandbox: true" src/main/index.ts && grep -q "webSecurity: true" src/main/index.ts && grep -q "loadURL" src/main/index.ts && ! grep -q "loadFile" src/main/index.ts && grep -q "ELECTRON_RENDERER_URL" src/main/index.ts && grep -q "setWindowOpenHandler" src/main/index.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "contextIsolation: true" src/main/index.ts` succeeds (FND-01 invariant 1)
    - `grep -q "nodeIntegration: false" src/main/index.ts` succeeds (FND-01 invariant 2)
    - `grep -q "sandbox: true" src/main/index.ts` succeeds (FND-01 invariant 3)
    - `grep -q "loadURL" src/main/index.ts` succeeds (FND-02 — never loadFile)
    - `! grep -q "loadFile" src/main/index.ts` succeeds (FND-02 — anti-pattern absent)
    - `grep -q "ELECTRON_RENDERER_URL" src/main/index.ts` succeeds (dev branch wired)
    - `grep -q "startRendererServer" src/main/index.ts` succeeds (production branch wired — implementation in 00-03)
    - `grep -q "initConfigStore" src/main/index.ts` succeeds (config store init wired — implementation in 00-04)
    - `grep -q "setWindowOpenHandler" src/main/index.ts` succeeds (window.open denial — defense in depth)
    - `grep -q "before-quit" src/main/index.ts` succeeds (renderer server cleanup wired)
  </acceptance_criteria>
  <done>Main entry point exists with the four FND-01 security flags literally present, the dev/packaged URL switch wired, and forward imports against contracts that 00-03 and 00-04 will satisfy.</done>
</task>

<task type="auto">
  <name>Task 2.3: Write src/main/ipc.ts (Phase 0 minimal handler set)</name>
  <files>src/main/ipc.ts</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 5 — the preload calls ipcRenderer.invoke('ping') and 'config:get-launch-count'; these are the channels we must register)
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 4 — getStore() returns the electron-store instance; we read launchCount from it)
  </read_first>
  <action>
    Write src/main/ipc.ts exporting `function registerIpc(): void`. Imports: ipcMain from 'electron'; getStore from './config-store.js' (forward reference to 00-04); IpcChannels from '../shared/ipc-channels.js'. Inside registerIpc(): call ipcMain.handle(IpcChannels.Ping, () => 'pong' as const) for the round-trip smoke test (the preload exposes this as window.api.ping). Call ipcMain.handle(IpcChannels.ConfigGetLaunchCount, () => { const store = getStore(); return store.get('launchCount'); }) — this returns the persisted launchCount used by the FND-03 verification path. Do NOT register any other channels in Phase 0 — every channel is attack surface, and Phase 3 will introduce the launcher channels later. Use the channel-name constants from IpcChannels (not raw strings) so a typo is a compile error.
  </action>
  <verify>
    <automated>test -f src/main/ipc.ts && grep -q "registerIpc" src/main/ipc.ts && grep -q "ipcMain.handle" src/main/ipc.ts && grep -q "IpcChannels.Ping" src/main/ipc.ts && grep -q "IpcChannels.ConfigGetLaunchCount" src/main/ipc.ts && grep -q "'pong'" src/main/ipc.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/ipc.ts` exists
    - `grep -q "export function registerIpc" src/main/ipc.ts` succeeds
    - `grep -q "IpcChannels.Ping" src/main/ipc.ts` succeeds (uses constants, not raw strings)
    - `grep -q "IpcChannels.ConfigGetLaunchCount" src/main/ipc.ts` succeeds
    - `grep -q "'pong'" src/main/ipc.ts` succeeds (ping returns the literal)
    - Channel count: `grep -c "ipcMain.handle" src/main/ipc.ts` returns 2 (exactly Ping + ConfigGetLaunchCount, no extras — Phase 0 minimality)
  </acceptance_criteria>
  <done>IPC bootstrap exists with exactly two handlers wired through the typed channel constants; main entry point (Task 2.2) can call registerIpc() at app.whenReady.</done>
</task>

</tasks>

<verification>
- `grep -q "contextIsolation: true" src/main/index.ts && grep -q "nodeIntegration: false" src/main/index.ts && grep -q "sandbox: true" src/main/index.ts` succeeds — FND-01 invariants literally present
- `! grep -q "loadFile" src/main/index.ts` succeeds — anti-pattern absent
- `grep -c "ipcMain.handle" src/main/ipc.ts` returns 2 — minimal surface
- After 00-03 and 00-04 also land, `npm run build` exits 0 (main compiles)
</verification>

<success_criteria>
- src/main/index.ts contains the four FND-01 security flags as greppable strings ("contextIsolation: true", "nodeIntegration: false", "sandbox: true", "webSecurity: true")
- src/main/index.ts contains NO call to mainWindow.loadFile() — only loadURL
- src/main/index.ts imports against the contracts published by 00-03 (startRendererServer/stopRendererServer) and 00-04 (initConfigStore)
- src/main/ipc.ts registers exactly two handlers via the typed IpcChannels constants
- Shared types/channels are positioned so preload (00-05) can import without circular references
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-bootstrap/00-02-SUMMARY.md` documenting:
- The literal webPreferences object that satisfies FND-01 (paste the object)
- The two IPC channels registered (with their handler return shapes)
- Forward references to 00-03 / 00-04 (which contracts this plan consumes that don't exist yet — main won't compile end-to-end until those two also land in wave 2)
</output>
</content>
</invoke>