---
phase: 00-foundation-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - electron.vite.config.ts
  - .gitignore
  - vitest.config.ts
  - playwright.config.ts
  - tests/e2e/secure-defaults.spec.ts
  - tests/e2e/http-protocol.spec.ts
  - tests/e2e/persistence.spec.ts
  - tests/unit/preload-api.test.ts
  - src/renderer/index.html
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
tags:
  - electron
  - scaffold
  - bootstrap
must_haves:
  truths:
    - "Running `npm install` in repo root completes without errors"
    - "`package.json` declares `\"type\": \"module\"` and pins electron to 35.7.5"
    - "Wave 0 test config + stub spec files exist so downstream plans can reference them"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with pinned versions and scripts"
      contains: "\"type\": \"module\""
    - path: "electron.vite.config.ts"
      provides: "Three-target electron-vite build config (main/preload/renderer)"
    - path: "vitest.config.ts"
      provides: "Vitest unit test config"
    - path: "playwright.config.ts"
      provides: "Playwright Electron e2e config"
    - path: "tests/e2e/secure-defaults.spec.ts"
      provides: "Wave 0 stub for FND-01 e2e assertion"
    - path: "tests/e2e/http-protocol.spec.ts"
      provides: "Wave 0 stub for FND-02 e2e assertion"
    - path: "tests/e2e/persistence.spec.ts"
      provides: "Wave 0 stub for FND-03 e2e assertion"
    - path: "tests/unit/preload-api.test.ts"
      provides: "Wave 0 stub for preload API shape unit test"
  key_links:
    - from: "package.json scripts"
      to: "electron-vite + vitest + playwright"
      via: "npm run dev / build / test / test:unit / test:e2e"
      pattern: "electron-vite (dev|build|preview)"
---

<objective>
Scaffold the AntiClicker repository with the exact electron-vite three-process layout described in 00-RESEARCH.md, pin every dependency to the versions verified there, and lay down Wave 0 test infrastructure (config files + stub specs) that later plans will fill in. This plan creates the foundation that 00-02, 00-03, 00-04 build on in parallel.

Purpose: Get a runnable `npm install` + `npm run typecheck` (no source code yet, but the toolchain works) and provide empty-but-existing test files so the per-task verification map in 00-VALIDATION.md resolves.

Output: A complete project skeleton with no application source yet (those land in 00-02 through 00-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/STATE.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/ROADMAP.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/REQUIREMENTS.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-VALIDATION.md
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md

<interfaces>
<!-- No prior code exists yet — this is the scaffolding plan. Downstream consumers (00-02..00-06) -->
<!-- will consume the directory layout and package.json scripts created here. -->

Directory layout (per 00-RESEARCH.md "Recommended Project Structure"):
- src/main/        — Electron main process
- src/preload/     — contextBridge preload
- src/renderer/    — Vite-built React renderer (Phase 0 placeholder)
- src/shared/      — types shared across processes
- tests/e2e/       — Playwright Electron e2e (FND-01/02/03)
- tests/unit/      — vitest unit tests
- out/             — build output (gitignored)

Scripts exposed in package.json:
- npm run dev        → electron-vite dev
- npm run build      → tsc --noEmit && electron-vite build
- npm run start      → electron-vite preview
- npm run typecheck  → tsc --noEmit (both node + web tsconfigs)
- npm run test       → vitest run (unit only — e2e via test:e2e)
- npm run test:unit  → vitest run
- npm run test:e2e   → playwright test
- npm run lint       → eslint --cache .
- npm run format     → prettier --write .
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Write package.json and tsconfig family</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, tsconfig.web.json, .gitignore</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 1 — package.json skeleton; Standard Stack table for pinned versions)
    - /Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md (project workflow rules)
  </read_first>
  <action>
    Create package.json at repo root with name "anticlicker", version "0.0.1", private:true, type:"module", main "./out/main/index.js". Pin dependencies exactly per 00-RESEARCH.md Standard Stack: dependencies block holds electron-store ^11.0.2 and zod ^4.0.0. devDependencies block holds electron ~35.7.5 (tilde — last 35.x only, never auto-bump to 36.x), electron-vite ^5.0.0, vite ^6.0.3, typescript ^5.7.0, react ^19.2.1, react-dom ^19.2.1, @vitejs/plugin-react ^5.1.1, @electron-toolkit/utils ^4.0.0, @playwright/test ^1.60.0, vitest ^4.1.6, eslint ^9.39.0, prettier ^3.7.4, @types/node ^22.7.7, @types/react ^19.2.7, @types/react-dom ^19.2.3. Scripts block: dev "electron-vite dev", build "tsc --noEmit && electron-vite build", start "electron-vite preview", typecheck "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json", test "vitest run", test:unit "vitest run", test:e2e "playwright test", lint "eslint --cache .", format "prettier --write .". Do NOT include electron-builder (deferred to Phase 6) or maplibre-gl / playwright runtime (deferred to Phase 1/4). Create tsconfig.json as the base (target ES2022, module ESNext, moduleResolution Bundler, strict true, esModuleInterop true, skipLibCheck true, noEmit true). Create tsconfig.node.json extending the base with lib ["ES2022"] and including src/main, src/preload, src/shared, electron.vite.config.ts. Create tsconfig.web.json extending the base with lib ["ES2022", "DOM", "DOM.Iterable"], jsx "react-jsx", and including src/renderer, src/shared. Create .gitignore ignoring node_modules/, out/, dist/, *.log, .env*, .env.local, .DS_Store, coverage/, playwright-report/, test-results/. Run `npm install` after files are written and confirm exit code 0.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.type!=='module')process.exit(1); if(!p.devDependencies.electron.startsWith('~35.7'))process.exit(2); if(p.dependencies['electron-store'].slice(0,3)!=='^11')process.exit(3); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists and `grep -q '"type": "module"' package.json` succeeds
    - `grep -q '"electron": "~35.7.5"' package.json` succeeds
    - `grep -q '"electron-store": "\\^11' package.json` succeeds
    - `grep -q '"zod": "\\^4' package.json` succeeds
    - Files `tsconfig.json`, `tsconfig.node.json`, `tsconfig.web.json`, `.gitignore` all exist
    - `node -e "require('./package.json')"` succeeds (file is valid JSON)
    - After `npm install`, `node_modules/electron/package.json` exists and reports version starting with "35.7"
  </acceptance_criteria>
  <done>Repo can run `npm install` cleanly; pinned versions match 00-RESEARCH.md Standard Stack; no electron-builder, maplibre-gl, or playwright (runtime) installed.</done>
</task>

<task type="auto">
  <name>Task 1.2: Write electron.vite.config.ts and create empty src/ tree</name>
  <files>electron.vite.config.ts, src/renderer/index.html, src/main/.gitkeep, src/preload/.gitkeep, src/shared/.gitkeep</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 8 — electron.vite.config.ts; Recommended Project Structure section)
  </read_first>
  <action>
    Create electron.vite.config.ts using the canonical three-target shape from Pattern 8: main target uses externalizeDepsPlugin() and outputs to out/main, preload uses externalizeDepsPlugin() and outputs to out/preload, renderer uses @vitejs/plugin-react with root "src/renderer", outDir "out/renderer", rollupOptions.input pointing at resolve('src/renderer/index.html'). Set resolve.alias `'@shared'` to resolve('src/shared') in both main and renderer targets. Create src/renderer/index.html as a minimal HTML5 doc with a `<div id="root"></div>` and a `<script type="module" src="/src/main.tsx"></script>` (the actual main.tsx will be created in plan 00-05; for now the file just needs to exist so the renderer build target has an entry). Create empty `.gitkeep` files in src/main, src/preload, src/shared so the directories exist for 00-02, 00-03, 00-04 to populate.
  </action>
  <verify>
    <automated>test -f electron.vite.config.ts && test -f src/renderer/index.html && test -d src/main && test -d src/preload && test -d src/shared && grep -q "externalizeDepsPlugin" electron.vite.config.ts && grep -q "outDir.*out/main" electron.vite.config.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - File `electron.vite.config.ts` exists at repo root
    - `grep -q "externalizeDepsPlugin" electron.vite.config.ts` succeeds (main + preload both use it)
    - `grep -q "out/main" electron.vite.config.ts` and `grep -q "out/preload" electron.vite.config.ts` and `grep -q "out/renderer" electron.vite.config.ts` all succeed
    - `grep -q "@vitejs/plugin-react" electron.vite.config.ts` succeeds (renderer uses React plugin)
    - Directories `src/main`, `src/preload`, `src/renderer`, `src/shared` all exist
    - File `src/renderer/index.html` contains `id="root"`
    - `npm run typecheck` exits 0 (no source files yet, just config is type-clean)
  </acceptance_criteria>
  <done>electron-vite config wires the three build targets; src/ directory tree exists empty for downstream plans to fill in.</done>
</task>

<task type="auto">
  <name>Task 1.3: Write Wave 0 test infrastructure (configs + stub specs)</name>
  <files>vitest.config.ts, playwright.config.ts, tests/e2e/secure-defaults.spec.ts, tests/e2e/http-protocol.spec.ts, tests/e2e/persistence.spec.ts, tests/unit/preload-api.test.ts</files>
  <read_first>
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-VALIDATION.md (Wave 0 Requirements section — exact list of test files needed)
    - /Users/aarnavk/Documents/Github/antiClicker/.planning/phases/00-foundation-bootstrap/00-RESEARCH.md (Pattern 7 — Playwright Electron e2e harness, defines the shape these stubs evolve into)
  </read_first>
  <action>
    Create vitest.config.ts using defineConfig from vitest/config with test.environment "node", test.include ["tests/unit/**/*.test.ts"], test.globals true. Create playwright.config.ts using defineConfig from @playwright/test with testDir "tests/e2e", timeout 60000, projects [{ name: "electron", testDir: "tests/e2e" }], use.trace "retain-on-failure". Create tests/e2e/secure-defaults.spec.ts as a test.skip stub with header comment "FND-01 — filled in by plan 00-06" and a single test.skip call importing `_electron as electron, expect, test from "@playwright/test"`. Create tests/e2e/http-protocol.spec.ts as a test.skip stub with header comment "FND-02 — filled in by plan 00-06". Create tests/e2e/persistence.spec.ts as a test.skip stub with header comment "FND-03 — filled in by plan 00-06". Create tests/unit/preload-api.test.ts as a test.skip stub importing from "vitest" with header comment "preload API shape — filled in by plan 00-06". All four spec files must compile under tsconfig.node.json (their imports must resolve). The stubs use test.skip so `npm run test` and `npm run test:e2e` pass green immediately — this is the Wave 0 invariant from 00-VALIDATION.md.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && test -f playwright.config.ts && test -f tests/e2e/secure-defaults.spec.ts && test -f tests/e2e/http-protocol.spec.ts && test -f tests/e2e/persistence.spec.ts && test -f tests/unit/preload-api.test.ts && npm run test:unit 2>&1 | grep -E "(0 failed|no tests|skip)" && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - All six files listed in `<files>` exist
    - `grep -q "test.skip" tests/e2e/secure-defaults.spec.ts` succeeds (it's a stub)
    - `grep -q "FND-01" tests/e2e/secure-defaults.spec.ts` succeeds (comment ties to requirement)
    - `grep -q "FND-02" tests/e2e/http-protocol.spec.ts` succeeds
    - `grep -q "FND-03" tests/e2e/persistence.spec.ts` succeeds
    - `grep -q "projects" playwright.config.ts` succeeds (electron project defined)
    - `npm run test:unit` exits 0 (stub passes — all tests are .skip)
  </acceptance_criteria>
  <done>Wave 0 test scaffolding complete; later plans (especially 00-06) fill in the test bodies; `nyquist_compliant` precondition met.</done>
</task>

</tasks>

<verification>
- `npm install` completes without errors
- `npm run typecheck` exits 0 (no source files yet but configs are clean)
- `npm run test:unit` exits 0 (stub specs all skipped)
- All directories/files in `files_modified` exist on disk
- `grep -q '"type": "module"' package.json` succeeds
</verification>

<success_criteria>
- A fresh clone followed by `npm install && npm run typecheck && npm run test:unit` returns three exit-0 commands
- Pinned versions in package.json match 00-RESEARCH.md Standard Stack exactly (Electron 35.7.5, electron-store 11.x, zod 4.x, electron-vite 5.x, Vite 6.x, TS 5.7.x, React 19.x, vitest 4.x, Playwright 1.60.x)
- Downstream plans 00-02, 00-03, 00-04 can land code into src/main, src/preload, src/shared without creating the directory structure
- 00-VALIDATION.md Wave 0 Requirements checklist is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-bootstrap/00-01-SUMMARY.md` documenting:
- Exact installed versions (paste output of `npm ls electron electron-vite vite typescript electron-store zod`)
- Any deviations from 00-RESEARCH.md (should be none)
- The fact that all four test files are .skip stubs (will be filled in by 00-06)
</output>
</content>
</invoke>