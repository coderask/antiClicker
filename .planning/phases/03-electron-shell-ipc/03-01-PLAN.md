---
phase: 03-electron-shell-ipc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ipc-channels.ts
  - src/main/ipc.ts
  - src/main/index.ts
autonomous: true
requirements:
  - REL-05
tags:
  - ipc
  - launcher
  - zod
  - electron-main
---

<objective>
Extend `src/main/ipc.ts` with launcher-backed handlers (launch/setGeo/close/list) wired to a
singleton launcher created in `src/main/index.ts`. Add zod validation on every renderer→main
payload. Wire the `instance-closed` push event. Add lifecycle cleanup on `app.before-quit`.
Also extend `src/shared/ipc-channels.ts` with the five new channel constants.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/03-electron-shell-ipc/03-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/launcher/index.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/shared/ipc-channels.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/main/ipc.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/main/index.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/shared/coords-schema.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend IPC channel constants</name>
  <files>src/shared/ipc-channels.ts</files>
  <action>
    Add five new channel constants to IpcChannels:
      LauncherLaunch: 'launcher:launch'
      LauncherSetGeo: 'launcher:set-geo'
      LauncherClose: 'launcher:close'
      LauncherList: 'launcher:list'
      LauncherInstanceClosed: 'launcher:instance-closed'
    Keep existing Ping and ConfigGetLaunchCount constants unchanged.
  </action>
  <done>IpcChannels object has all 7 keys; `as const` preserves literal types; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend main IPC handlers with launcher integration</name>
  <files>src/main/ipc.ts</files>
  <action>
    Rewrite src/main/ipc.ts to:
    1. Import createLauncher from '../launcher/index.js', BrowserWindow from 'electron', z from 'zod',
       ZodCoordsSchema from '../shared/coords-schema.js'.
    2. Module-scope singleton: `let _launcher: Launcher | null = null;`
       getLauncher() initializer: creates if null, returns instance.
       Export closeLauncherIfAny(): Promise<void> that calls _launcher.closeAll() if non-null.
    3. Export composite schemas (for unit tests):
       LaunchPayloadSchema = ZodCoordsSchema (same shape)
       SetGeoPayloadSchema = z.object({ id: z.string(), coords: ZodCoordsSchema })
       ClosePayloadSchema = z.object({ id: z.string() })
    4. In registerIpc():
       - Keep existing Ping and ConfigGetLaunchCount handlers unchanged.
       - Add ipcMain.handle(IpcChannels.LauncherLaunch, ...) with ZodCoordsSchema.parse(payload)
       - Add ipcMain.handle(IpcChannels.LauncherSetGeo, ...) with SetGeoPayloadSchema.parse(payload)
       - Add ipcMain.handle(IpcChannels.LauncherClose, ...) with ClosePayloadSchema.parse(payload)
       - Add ipcMain.handle(IpcChannels.LauncherList, ...) calling getLauncher().list()
    5. Wire instance-closed push: inside registerIpc(), after handler registration,
       getLauncher().on('instance-closed', (id) => {
         BrowserWindow.getAllWindows()[0]?.webContents.send(IpcChannels.LauncherInstanceClosed, id);
       });
    Note: All payload params must be typed as `unknown` to enforce Zod parsing.
  </action>
  <done>
    - ipc.ts exports: registerIpc, closeLauncherIfAny, LaunchPayloadSchema, SetGeoPayloadSchema, ClosePayloadSchema
    - All four launcher handlers registered in registerIpc()
    - Zod validates every payload before any launcher call
    - instance-closed listener wired once in registerIpc()
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire launcher lifecycle in main/index.ts</name>
  <files>src/main/index.ts</files>
  <action>
    Update src/main/index.ts:
    1. Import closeLauncherIfAny from './ipc.js' (alongside existing registerIpc import).
    2. In app.whenReady(): ensure getLauncher() is called (by calling registerIpc() which calls
       getLauncher() inside) before createWindow(). The current order already does this.
    3. Update the before-quit handler to also call closeLauncherIfAny():
       app.on('before-quit', async () => {
         await stopRendererServer();
         await closeLauncherIfAny();
       });
  </action>
  <done>
    - before-quit calls closeLauncherIfAny() in addition to stopRendererServer()
    - npm run typecheck passes
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- IpcChannels has 7 keys including all 5 new launcher channels
- registerIpc() registers 6 handlers total (ping, getLaunchCount, launch, setGeo, close, list)
- LaunchPayloadSchema, SetGeoPayloadSchema, ClosePayloadSchema are exported from ipc.ts
- closeLauncherIfAny is exported from ipc.ts and imported in index.ts
</verification>
