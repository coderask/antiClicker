---
phase: 03-electron-shell-ipc
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - tests/unit/ipc-validation.test.ts
  - tests/unit/preload-api.test.ts
  - tests/e2e/launch-flow.spec.ts
autonomous: true
requirements:
  - REL-05
tags:
  - tests
  - vitest
  - playwright
  - e2e
---

<objective>
Write the ipc-validation unit test (vitest, pure Node) that asserts Zod schemas reject
bad payloads without launching Electron. Write the launch-flow e2e test (Playwright
_electron.launch) that proves the full IPC chain works: button click → Chrome window →
live-instances counter → close → counter decrements. Update the preload-api unit test to
reflect the expanded Api surface (7 methods).
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/03-electron-shell-ipc/03-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/tests/e2e/secure-defaults.spec.ts
@/Users/aarnavk/Documents/Github/antiClicker/tests/launcher/fixture.ts
@/Users/aarnavk/Documents/Github/antiClicker/tests/unit/preload-api.test.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/main/ipc.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod IPC validation unit test</name>
  <files>tests/unit/ipc-validation.test.ts</files>
  <action>
    Create tests/unit/ipc-validation.test.ts using vitest.
    Import LaunchPayloadSchema, SetGeoPayloadSchema, ClosePayloadSchema from
    '../../src/main/ipc.js'.
    Test cases (use safeParse, assert result.success):
    1. Valid launch payload: { latitude: 35.6762, longitude: 139.6503 } → success true
    2. Invalid latitude (out of range 95): { latitude: 95, longitude: 0 } → success false
    3. Invalid longitude type ("foo"): { latitude: 0, longitude: "foo" } → success false
    4. Missing id on close payload: {} → success false
    5. Valid setGeo payload: { id: "abc", coords: { latitude: 0, longitude: 0 } } → success true
    6. Invalid setGeo (coords out of range): { id: "abc", coords: { latitude: 91, longitude: 0 } } → success false
  </action>
  <done>
    - 6 test cases, all pass
    - vitest run tests/unit/ipc-validation.test.ts exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Update preload-api unit test</name>
  <files>tests/unit/preload-api.test.ts</files>
  <action>
    Update tests/unit/preload-api.test.ts to reflect the expanded Api surface.
    The Api now has 7 keys: ping, getLaunchCount, launch, setGeo, close, list, onInstanceClosed.
    Update the test that asserts the count to expect 7.
    Update the fakeApi type to include all 7 methods (can use stub implementations).
    Keep the existing ping and getLaunchCount behavior tests.
  </action>
  <done>
    - All existing tests still pass
    - Assertion updated from 2 keys to 7 keys
    - Fake Api implementations cover all 7 methods
  </done>
</task>

<task type="auto">
  <name>Task 3: E2E launch-flow spec</name>
  <files>tests/e2e/launch-flow.spec.ts</files>
  <action>
    Create tests/e2e/launch-flow.spec.ts using @playwright/test with _electron.
    Flow:
    1. Launch app: electron.launch({ args: [join(process.cwd(), 'out/main/index.js')] })
    2. Get firstWindow.
    3. Wait for [data-testid="ping"] to have text "pong" (10s timeout, confirms app is ready).
    4. Click [data-testid="launch-button"].
    5. Wait for [data-testid="live-instances"] to have text "1" (15s timeout — Chrome launch can take a few seconds).
    6. Via window.evaluate, call window.api.list() and assert returned array has length 1.
       Also assert list()[0].coords.latitude is approximately 35.6762.
    7. Get the instance id from list()[0].id.
    8. Via window.evaluate, call window.api.close(id) on that id.
    9. Wait for [data-testid="live-instances"] to have text "0" (10s timeout).
    10. await app.close().

    Notes:
    - headless: true for the spawned Chrome will NOT work unless the launcher is called with
      headless option — but the UI button uses the default (headless: false). Accept that the
      test spawns a visible Chrome window. This is expected behavior.
    - Wrap in try/finally to always close the app.
    - Use test.setTimeout(90_000) for this test.
  </action>
  <done>
    - Launch-flow spec file exists in tests/e2e/
    - Test covers the full IPC round-trip: launch → counter → list → close → counter
    - npm run test:e2e includes this spec
  </done>
</task>

</tasks>

<verification>
- `npm run test:unit` passes all unit tests including new ipc-validation tests
- `npm run test:e2e` passes all e2e tests including launch-flow spec
- launch-flow spec correctly asserts live-instances counter increments and decrements
</verification>
