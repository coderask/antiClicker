---
phase: 03-electron-shell-ipc
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/renderer/src/App.tsx
autonomous: true
requirements:
  - REL-05
tags:
  - renderer
  - react
  - ui
---

<objective>
Update `src/renderer/src/App.tsx` with a "Launch at Tokyo" button and a "live instances: N"
readout. Wire window.api.onInstanceClosed to decrement the count. Keep existing Phase 0
verification rows (protocol, ping, launchCount) intact.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/03-electron-shell-ipc/03-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/App.tsx
@/Users/aarnavk/Documents/Github/antiClicker/src/preload/index.d.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launcher section to App.tsx</name>
  <files>src/renderer/src/App.tsx</files>
  <action>
    Extend App.tsx:
    1. Add useState<number>(0) for liveCount.
    2. Add useState<string | null>(null) for launchError.
    3. In useEffect (separate from existing one or combined), call window.api.onInstanceClosed
       with a callback that decrements liveCount. Return the unsubscribe fn as cleanup.
    4. Add a handleLaunch async function:
       - Calls window.api.launch({ latitude: 35.6762, longitude: 139.6503 })
       - On success: increments liveCount, clears launchError
       - On error: sets launchError to error message
    5. Add UI section below existing dl:
       <section>
         <h2>Launcher</h2>
         <button data-testid="launch-button" onClick={handleLaunch}>
           Launch at Tokyo (35.6762, 139.6503)
         </button>
         {launchError && <p data-testid="launch-error" style={{ color: 'red' }}>{launchError}</p>}
         <dl>
           <dt>live instances</dt>
           <dd data-testid="live-instances">{liveCount}</dd>
         </dl>
       </section>
    Keep all existing Phase 0 content exactly as-is.
  </action>
  <done>
    - "Launch at Tokyo" button exists with data-testid="launch-button"
    - data-testid="live-instances" shows current count
    - onInstanceClosed properly decrements count and returns unsubscribe
    - npm run typecheck (tsconfig.web.json) passes
  </done>
</task>

</tasks>

<verification>
- App.tsx renders a button[data-testid="launch-button"]
- App.tsx renders dd[data-testid="live-instances"]
- Clicking the button calls window.api.launch with Tokyo coords
- onInstanceClosed subscription is set up in useEffect with cleanup
- All existing Phase 0 data-testid attributes still present: protocol, ping, launch-count
</verification>
