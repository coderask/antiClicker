---
phase: 03-electron-shell-ipc
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements:
  - REL-05
tags:
  - preload
  - contextbridge
  - ipc
---

<objective>
Extend `src/preload/index.ts` to expose the four launcher invoke methods (launch, setGeo,
close, list) plus the `onInstanceClosed` subscription through contextBridge. Keep the
existing ping and getLaunchCount methods. Update the ambient type declaration in
`src/preload/index.d.ts` so `window.api` has the correct type everywhere in the renderer.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/03-electron-shell-ipc/03-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/preload/index.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/preload/index.d.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/shared/ipc-channels.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/launcher/index.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend preload contextBridge surface</name>
  <files>src/preload/index.ts</files>
  <action>
    Rewrite src/preload/index.ts to expose all 7 api members:
      ping, getLaunchCount (existing, keep unchanged)
      launch(coords): calls ipcRenderer.invoke(IpcChannels.LauncherLaunch, coords)
        coords shape: { latitude: number; longitude: number; accuracy?: number }
      setGeo(id, coords): calls ipcRenderer.invoke(IpcChannels.LauncherSetGeo, { id, coords })
      close(id): calls ipcRenderer.invoke(IpcChannels.LauncherClose, { id })
      list(): calls ipcRenderer.invoke(IpcChannels.LauncherList)
      onInstanceClosed(cb): registers ipcRenderer.on listener, returns unsubscribe fn
        cb signature: (id: string) => void
        returns: () => void

    Import IpcRendererEvent from electron for the listener type.
    Keep `export type Api = typeof api` and the contextIsolated guard unchanged.
    No other changes — do not add any extra properties.
  </action>
  <done>
    - api object has exactly 7 keys
    - onInstanceClosed returns () => void (unsubscribe)
    - export type Api = typeof api covers all 7 methods
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ambient window type</name>
  <files>src/preload/index.d.ts</files>
  <action>
    The ambient `src/preload/index.d.ts` already imports `Api` from `./index` and declares
    `Window.api: Api`. Since `Api` is inferred from the api object, adding methods to
    index.ts automatically widens the type. Verify the file content is correct and does
    not need changes. If it does, update it to match.
  </action>
  <done>
    - index.d.ts correctly re-exports the widened Api type
    - Renderer code can call window.api.launch({...}) without type errors
    - npm run typecheck (tsconfig.web.json) passes
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0 (both tsconfig.node.json and tsconfig.web.json)
- window.api in renderer has 7 methods: ping, getLaunchCount, launch, setGeo, close, list, onInstanceClosed
- onInstanceClosed return type is () => void (usable as useEffect cleanup)
</verification>
