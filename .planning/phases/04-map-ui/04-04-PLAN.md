---
phase: 04-map-ui
plan: 04
type: execute
wave: 1
depends_on: [04-03]
files_modified:
  - tests/unit/parseMapsUrl.test.ts
  - tests/renderer/CoordInput.test.tsx
  - tests/e2e/map-flow.spec.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
tags:
  - tests
  - unit
  - e2e
  - vitest
  - playwright
---

<objective>
Write tests covering:
1. parseMapsUrl unit tests (vitest) — 6+ cases for each URL format + malformed inputs
2. CoordInput component tests (vitest + happy-dom) — renders form, validates invalid lat, validates
   valid lat/lng fires callback, URL paste populates inputs
3. map-flow e2e test (Playwright Electron) — map renders, click drops pin, coords update,
   launch-here triggers IPC, live-instances increments
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/04-map-ui/04-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/utils/parseMapsUrl.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/CoordInput.tsx
@/Users/aarnavk/Documents/Github/antiClicker/tests/e2e/launch-flow.spec.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: parseMapsUrl unit tests</name>
  <files>tests/unit/parseMapsUrl.test.ts</files>
  <action>
    Create tests/unit/parseMapsUrl.test.ts using vitest.

    Cases to cover (minimum 8):
    1. /maps/@lat,lng,zoom z format → returns correct coords
    2. /maps/@lat,lng,zoom.decimal z/data=... format → returns correct coords
    3. /maps/place/Name/@lat,lng,zoom z → returns correct coords
    4. maps.google.com/?q=lat,lng → returns correct coords
    5. /maps?q=lat,lng&z=12 → returns correct coords
    6. /maps?ll=lat,lng → returns correct coords
    7. Non-Google Maps URL → returns null
    8. URL with no recognizable lat/lng pattern → returns null
    9. URL with lat > 90 → returns null (out of WGS-84 bounds)
    10. Empty string → returns null
    11. Non-URL string → returns null
    12. Google Maps URL with negative coordinates (e.g., San Francisco) → returns correct coords
  </action>
  <done>Test file exists; all cases listed above are implemented; npm run test:unit passes.</done>
</task>

<task type="auto">
  <name>Task 2: CoordInput component tests</name>
  <files>tests/renderer/CoordInput.test.tsx</files>
  <action>
    Create tests/renderer/CoordInput.test.tsx using vitest + @testing-library/react + happy-dom.

    Check what testing-library packages are installed. If @testing-library/react is not present,
    use vitest's built-in rendering with happy-dom (set environment in vitest.config or use
    a inline // @vitest-environment happy-dom comment).

    Test cases:
    1. "renders the form inputs" — render <CoordInput onSubmit={vi.fn()} />, assert lat-input
       and lng-input are in the document.
    2. "shows error on invalid latitude" — type "999" in lat-input, "0" in lng-input,
       submit form, assert data-testid="coord-error" is visible with an error message.
    3. "calls onSubmit with valid coords" — type "35.6762" in lat-input, "139.6503" in lng-input,
       submit form, assert onSubmit was called with { latitude: 35.6762, longitude: 139.6503 }.
    4. "populates lat/lng from valid Google Maps URL" — type/paste a valid URL into
       maps-url-input, assert lat-input and lng-input values are populated.
    5. "shows error on unparseable URL" — type an invalid URL (e.g., "https://example.com")
       into maps-url-input, assert coord-error is visible.

    Setup: install @testing-library/react if needed, or use vitest's happy-dom environment.
    Check vitest.config.ts for current environment setting.
  </action>
  <done>Test file exists; all 5 cases pass; npm run test:unit passes.</done>
</task>

<task type="auto">
  <name>Task 3: map-flow e2e test</name>
  <files>tests/e2e/map-flow.spec.ts</files>
  <action>
    Create tests/e2e/map-flow.spec.ts using @playwright/test electron fixture.

    test.setTimeout(120_000) — map load + Chrome launch can take time.

    Flow:
    1. Launch built Electron app: electron.launch({ args: [join(cwd, 'out/main/index.js')] })
    2. Get firstWindow().
    3. Wait for ping to be "pong" (app ready).
    4. Assert data-testid="pin-coords" has text '' (empty — no pin yet).
    5. Assert data-testid="launch-here-button" is disabled.
    6. Simulate a click on the map canvas to drop a pin:
       await window.locator('canvas').click({ position: { x: 600, y: 400 } })
    7. Assert pin-coords has text that is not empty (within 5s).
       Use: await expect(window.locator('[data-testid="pin-coords"]')).not.toHaveText('', { timeout: 5000 })
    8. Assert launch-here-button is enabled (not disabled).
    9. Click the launch-here-button.
    10. Assert live-instances reaches "1" within 30s.
    11. Close the app.

    Note: wrap in try/finally to guarantee app.close().
  </action>
  <done>Test file exists; structure matches the flow above; imports are correct.</done>
</task>

</tasks>

<verification>
  Run: npm run test:unit — parseMapsUrl.test.ts and CoordInput.test.tsx pass.
  Run: npm run test:e2e — map-flow.spec.ts passes (full build required).
  Run: npm run test — all tests green (unit + launcher + e2e).
</verification>

<success_criteria>
  1. parseMapsUrl unit tests: 8+ cases, all pass.
  2. CoordInput component tests: 5 cases, all pass.
  3. map-flow e2e: canvas click drops pin, launch-here triggers IPC, live-instances increments.
  4. npm run test passes (no regressions in prior phases).
</success_criteria>
