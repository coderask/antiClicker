---
phase: 04-map-ui
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/renderer/src/App.tsx
autonomous: true
requirements:
  - MAP-02
  - MAP-05
  - MAP-06
tags:
  - pin
  - ipc
  - renderer
---

<objective>
Restructure App.tsx: lift pin state into App, render MapView as the primary content,
add a pin coordinate readout with data-testid="pin-coords", add a "Launch here" button
with data-testid="launch-here-button" that wires to window.api.launch. Preserve all
Phase 0 and Phase 3 data-testid attributes in a collapsed <details> footer.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/04-map-ui/04-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/App.tsx
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/map/MapView.tsx
@/Users/aarnavk/Documents/Github/antiClicker/src/preload/index.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure App.tsx with map + pin state</name>
  <files>src/renderer/src/App.tsx</files>
  <action>
    Rewrite App.tsx to:

    1. Lift pin state: useState<{ latitude: number; longitude: number } | null>(null)
    2. Keep all Phase 0 state: pong, launchCount, protocol
    3. Keep Phase 3 state: liveCount, launchError
    4. Keep both useEffects unchanged (ping/getLaunchCount + onInstanceClosed subscription)

    Layout (full-viewport, no padding on main):
      <main style={{ display: 'flex', flexDirection: 'column', height: '100vh', margin: 0 }}>

        {/* Map fills available space */}
        <div style={{ flex: 1, position: 'relative' }}>
          <MapView pin={pin} onPinChange={setPin} />
        </div>

        {/* Bottom bar: coords readout + launch button + live instances */}
        <div style={{ padding: '8px 16px', background: '#1a1a2e', color: '#eee', display: 'flex', gap: 16, alignItems: 'center' }}>
          <span data-testid="pin-coords">
            {pin ? `${pin.latitude.toFixed(4)}, ${pin.longitude.toFixed(4)}` : ''}
          </span>
          <button
            data-testid="launch-here-button"
            disabled={pin === null || launchLoading}
            onClick={() => void handleLaunch()}
            style={{ padding: '6px 14px', cursor: pin ? 'pointer' : 'default' }}
          >
            {launchLoading ? 'Launching...' : 'Launch here'}
          </button>
          {launchError && (
            <span data-testid="launch-error" style={{ color: '#ff6b6b' }}>{launchError}</span>
          )}
          <span style={{ marginLeft: 'auto' }}>
            live: <span data-testid="live-instances">{liveCount}</span>
          </span>
        </div>

        {/* Phase 0 verification rows — hidden by default, testids preserved */}
        <details style={{ padding: '4px 16px', background: '#111', color: '#888', fontSize: 12 }}>
          <summary>Debug info (Phase 0)</summary>
          <dl style={{ margin: 0 }}>
            <dt>window.location.protocol</dt>
            <dd data-testid="protocol">{protocol}</dd>
            <dt>window.api.ping()</dt>
            <dd data-testid="ping">{pong ?? '…'}</dd>
            <dt>electron-store launchCount</dt>
            <dd data-testid="launch-count">{launchCount ?? '…'}</dd>
          </dl>
        </details>
      </main>

    handleLaunch:
      - Add launchLoading state (boolean)
      - Set launchLoading true before await
      - Call window.api.launch({ latitude: pin.latitude, longitude: pin.longitude })
      - On success: increment liveCount, clear error
      - On error: set launchError
      - Always: set launchLoading false in finally
      - Guard: if (!pin) return
  </action>
  <done>
    App.tsx compiles. MapView is rendered. pin-coords testid is present and shows empty string
    when no pin. launch-here-button testid is present and disabled when pin is null.
    All Phase 0 testids (protocol, ping, launch-count) and Phase 3 testids (live-instances,
    launch-error) are still in the DOM. typecheck passes.
  </done>
</task>

</tasks>

<verification>
  Run: npm run typecheck — exits 0.
  All Phase 0 data-testid attributes queryable: protocol, ping, launch-count.
  Phase 3 data-testid attributes: live-instances, launch-error.
  New Phase 4 data-testid attributes: pin-coords, launch-here-button.
</verification>

<success_criteria>
  1. MapView is the primary content in App.tsx (fills viewport).
  2. pin state is lifted into App.tsx.
  3. data-testid="pin-coords" shows lat/lng when pin is set, empty string when not.
  4. data-testid="launch-here-button" is disabled when pin is null; clicking calls window.api.launch.
  5. All prior data-testid attributes remain queryable.
  6. npm run typecheck passes.
</success_criteria>
