---
phase: 01-cdp-cli-primitive
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/cli/geolocation.spec.ts
autonomous: true
requirements:
  - LCH-02
  - LCH-03
tags:
  - playwright
  - integration-test
  - geolocation
  - hermetic
---

<objective>
Create a hermetic Playwright integration test that launches a real Chromium context with geolocation overridden, verifies the spoof via a data: URL fixture (no external network), and tests that the spoof survives cross-origin navigation (Pitfall 2 closed).
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/01-cdp-cli-primitive/01-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/.planning/research/PITFALLS.md

Key constraints:
- Use data: URLs for fixture HTML — no external network calls (hermetic test)
- Must verify: no permission prompt appears (permissions granted at context creation)
- Must verify: cross-origin navigation does not break the spoof
- Must clean up context and userDataDir after each test
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Create tests/cli/geolocation.spec.ts — hermetic Playwright integration test</name>
  <files>tests/cli/geolocation.spec.ts</files>
  <action>
    Create tests/cli/geolocation.spec.ts using @playwright/test:

    The fixture HTML (as a data: URL string):
    ```html
    <!DOCTYPE html><html><body>
    <div id="result">pending</div>
    <script>
      navigator.geolocation.getCurrentPosition(
        pos => { document.getElementById('result').textContent = JSON.stringify({lat: pos.coords.latitude, lng: pos.coords.longitude}); },
        err => { document.getElementById('result').textContent = 'error:' + err.code; }
      );
    </script>
    </body></html>
    ```
    Encode this as a data:text/html;charset=utf-8,<url-encoded-content> URL.

    Test setup (beforeAll or per-test):
    - Create userDataDir = mkdtempSync(join(tmpdir(), 'anticlicker-test-'))
    - Launch context with chromium.launchPersistentContext(userDataDir, { headless: true, geolocation: { latitude: 35.6762, longitude: 139.6503, accuracy: 10 }, permissions: ['geolocation'] })

    Tests:
    1. "reports spoofed coordinates via getCurrentPosition":
       - context.newPage(), navigate to the fixture data: URL
       - Wait for #result to NOT contain 'pending' (waitForFunction or locator.waitFor)
       - Assert #result text parses to { lat: 35.6762, lng: 139.6503 }

    2. "no geolocation permission prompt appears (permission pre-granted)":
       - context.newPage()
       - Set up a dialog handler that fails the test if any dialog appears: page.on('dialog', () => test.fail())
       - Navigate to fixture data: URL
       - Wait for #result to resolve (not 'pending', not 'error:1')
       - Assert result is the spoofed coords (PERMISSION_DENIED = error code 1)

    3. "spoof survives cross-origin navigation (context-scoped override)":
       - context.newPage()
       - Navigate to first fixture data: URL, assert coords match
       - Navigate the SAME page to about:blank (different origin)
       - Use page.evaluate to call getCurrentPosition via a Promise wrapper:
         ```ts
         const result = await page.evaluate(() => new Promise<{lat: number, lng: number}>((resolve, reject) => {
           navigator.geolocation.getCurrentPosition(
             pos => resolve({ lat: pos.coords.latitude, lng: pos.coords.longitude }),
             err => reject(new Error('geo error: ' + err.code))
           );
         }));
         ```
       - Assert result.lat === 35.6762 and result.lng === 139.6503

    Teardown (afterAll):
    - await context.close()
    - rmSync(userDataDir, { recursive: true, force: true })

    Use headless: true for the integration test (visual verification is done manually / in the CLI demo; CI tests must not require a display).

    The test file must import: { test, expect, chromium } from '@playwright/test'; { mkdtempSync, rmSync } from 'node:fs'; { tmpdir } from 'node:os'; { join } from 'node:path'.
  </action>
  <verify>
    <automated>test -f /Users/aarnavk/Documents/Github/antiClicker/tests/cli/geolocation.spec.ts && grep -q "launchPersistentContext" /Users/aarnavk/Documents/Github/antiClicker/tests/cli/geolocation.spec.ts && grep -q "cross-origin" /Users/aarnavk/Documents/Github/antiClicker/tests/cli/geolocation.spec.ts && echo ok</automated>
  </verify>
  <done>tests/cli/geolocation.spec.ts exists with 3 tests: spoof verification, no-prompt verification, and cross-origin nav survival. All hermetic (data: URLs only).</done>
</task>

</tasks>

<success_criteria>
- tests/cli/geolocation.spec.ts exists with 3 tests
- All tests pass when run via: npx playwright test tests/cli/geolocation.spec.ts
- Tests are hermetic — no external network calls
- Cross-origin navigation test explicitly validates Pitfall 2 is closed
- Temporary profile directories are deleted after each test run
</success_criteria>
