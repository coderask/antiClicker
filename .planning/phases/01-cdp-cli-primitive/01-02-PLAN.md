---
phase: 01-cdp-cli-primitive
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - tests/cli/argv.test.ts
autonomous: true
requirements:
  - LCH-01
tags:
  - vitest
  - unit-tests
  - cli
  - argv
---

<objective>
Create unit tests for the argv parsing and bounds validation logic of cli-prototype.ts. These tests run under Vitest (no browser launch) and verify that the CLI correctly handles valid/invalid inputs without touching Playwright at all.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/01-cdp-cli-primitive/01-RESEARCH.md

The unit tests should import testable functions directly from the CLI module or, since the CLI module calls process.exit(), extract the parsing/validation logic into a helper that tests can call without triggering exit. The cleanest approach: extract a pure `parseCliArgs(argv: string[]): { lat: number; lng: number }` function that throws on invalid input, which the main() entrypoint calls and wraps with the process.exit() calls. Tests call parseCliArgs directly.
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Extract parseCliArgs helper + create tests/cli/argv.test.ts</name>
  <files>
    scripts/cli-prototype.ts
    tests/cli/argv.test.ts
  </files>
  <action>
    Step A: Refactor scripts/cli-prototype.ts to extract a testable parseCliArgs function:
    - Export a named function `parseCliArgs(argv: string[]): { lat: number; lng: number }` that:
      - Calls parseArgs with the given argv slice
      - Returns { lat, lng } as numbers if valid
      - Throws an Error with a descriptive message if lat/lng missing, NaN, or out of bounds
    - The main() function (or top-level async IIFE) calls parseCliArgs(process.argv.slice(2)) and wraps with try/catch → stderr + process.exit(1).

    Step B: Create tests/cli/argv.test.ts:
    - Import { parseCliArgs } from '../../scripts/cli-prototype.js'
    - Test cases (use vitest describe/it/expect):
      1. Valid coords: parseCliArgs(['--lat', '35.6762', '--lng', '139.6503']) returns { lat: 35.6762, lng: 139.6503 }
      2. Missing --lat: throws with message including 'lat'
      3. Missing --lng: throws with message including 'lng'
      4. Non-numeric lat: parseCliArgs(['--lat', 'foo', '--lng', '0']) throws
      5. Non-numeric lng: parseCliArgs(['--lat', '0', '--lng', 'bar']) throws
      6. Lat out of range (too high): parseCliArgs(['--lat', '91', '--lng', '0']) throws with message including '90'
      7. Lat out of range (too low): parseCliArgs(['--lat', '-91', '--lng', '0']) throws
      8. Lng out of range (too high): parseCliArgs(['--lat', '0', '--lng', '181']) throws with message including '180'
      9. Lng out of range (too low): parseCliArgs(['--lat', '0', '--lng', '-181']) throws
      10. Boundary values accepted: lat=90, lng=180 — no throw
      11. Boundary values accepted: lat=-90, lng=-180 — no throw
      12. Zero coords: parseCliArgs(['--lat', '0', '--lng', '0']) returns { lat: 0, lng: 0 }
  </action>
  <verify>
    <automated>test -f /Users/aarnavk/Documents/Github/antiClicker/tests/cli/argv.test.ts && grep -q "parseCliArgs" /Users/aarnavk/Documents/Github/antiClicker/tests/cli/argv.test.ts && echo ok</automated>
  </verify>
  <done>tests/cli/argv.test.ts exists with 12+ test cases covering valid/invalid inputs and boundary values. parseCliArgs is exported from cli-prototype.ts and callable without browser launch.</done>
</task>

</tasks>

<success_criteria>
- tests/cli/argv.test.ts exists with at least 10 test cases
- npx vitest run tests/cli/argv.test.ts passes with all tests green
- No browser is launched by any unit test
- parseCliArgs is exported from cli-prototype.ts
</success_criteria>
