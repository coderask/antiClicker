---
phase: 06-verification-package
plan: 05
type: execute
wave: 1
depends_on: [06-04]
files_modified:
  - tests/unit/sweep.test.ts
  - tests/e2e/verify-flow.spec.ts
  - tests/e2e/first-run-overlay.spec.ts
  - README.md
autonomous: true
requirements:
  - VRF-01
  - VRF-02
  - VRF-03
  - REL-01
tags:
  - tests
  - packaging
  - readme
---

<objective>
1. Write unit tests for the orphan sweep function.
2. Write e2e test for verify-flow (using a localhost fixture, not browserleaks).
3. Write e2e test for first-run-overlay.
4. Run `npm run package` and capture the output — both .dmg and .exe must appear in dist/.
5. Create README.md with install instructions and warnings.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/06-verification-package/06-RESEARCH.md
</context>

<tasks>

<task id="1" type="auto">
### Unit test: orphan profile sweep

Create `tests/unit/sweep.test.ts`:
- Export the sweepOrphanedProfiles function from index.ts (or extract to a utility)
- Test: dead PID dir is removed
- Test: alive PID dir is preserved
- Test: dir without pid.txt is removed (orphaned)
- Test: non-anticlicker dirs are ignored
- Mock `readdirSync`, `rmSync`, `readFileSync`, `existsSync`, `process.kill`
</task>

<task id="2" type="auto">
### E2E test: verify-flow

Create `tests/e2e/verify-flow.spec.ts`:
- Launch the Electron app via `electron.launch()`
- Navigate to the app
- Set a draft pin via the CoordInput or CoordInput fields
- Click "Launch here" button
- Wait for an instance row to appear in sidebar (`data-testid="instance-row"` or similar)
- Click the `verify-{id}` button on that row
- Assert that a result element appears with testid `verify-result-{id}` containing "match" text or similar

Note: The verify button calls `navigator.geolocation.getCurrentPosition` in the launched Chrome. Since the launched Chrome IS spoofed with the pin coords, this should return a match. Use a headless launched Chrome in tests.
</task>

<task id="3" type="auto">
### E2E test: first-run-overlay

Create `tests/e2e/first-run-overlay.spec.ts`:
- Launch the app with a fresh `--user-data-dir` (so firstRunSeen = false)
- Assert overlay is visible: `[data-testid="scope-overlay"]` is present
- Click `[data-testid="scope-overlay-dismiss"]`
- Assert overlay disappears
- Close the app, relaunch with SAME user-data-dir
- Assert overlay does NOT appear (firstRunSeen = true persisted)
</task>

<task id="4" type="auto">
### Package build (dry-run)

Run `npm run package` and capture the dist/ directory contents.
This will:
1. Build the app (electron-vite build)
2. Run electron-builder --mac --win
3. Produce dist/ with .dmg and .exe artifacts

Document the results.
</task>

<task id="5" type="auto">
### Create README.md

Create `README.md` at the repo root:
- Project description (1 paragraph)
- Screenshot placeholder: `![AntiClicker screenshot](docs/screenshot.png)` *(add screenshot after install)*
- Quick-start: Download → Install → Click pin on map → Launch Chrome → Chrome is at that location
- Security warnings: Gatekeeper (macOS), SmartScreen (Windows) — right-click → Open workaround
- Build from source: `npm install && npx playwright install chromium && npm run dev`
- Package locally: `npm run package` → artifacts in `dist/`
- Keep under 200 lines
</task>

</tasks>

<verification>
- `npm run test:unit` green (includes sweep.test.ts)
- `npm run test:e2e` green (includes verify-flow + first-run-overlay)
- `dist/` contains .dmg and .exe artifacts with non-zero sizes
- README.md exists at repo root
</verification>

<done_criteria>
All tests green; packaging artifacts in dist/; README.md written.
</done_criteria>
