---
phase: 06-verification-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/config-store.ts
  - src/main/index.ts
  - src/launcher/index.ts
  - src/shared/ipc-channels.ts
autonomous: true
requirements:
  - REL-04
  - FND-04
tags:
  - config
  - sweep
  - firstrun
---

<objective>
1. Add `firstRunSeen: boolean` (default false) to ConfigSchema in config-store.ts.
2. Write a `pid.txt` sentinel file into each new profile dir on instance launch in launcher/index.ts.
3. On app startup (app.whenReady in index.ts), sweep os.tmpdir() for orphaned anticlicker-profile-* dirs: for each, read pid.txt, call process.kill(pid, 0), delete the dir if PID is dead.
4. Add IpcChannels.ConfigMarkFirstRunSeen channel name to shared/ipc-channels.ts.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/06-verification-package/06-RESEARCH.md
</context>

<tasks>

<task id="1" type="auto">
### Add firstRunSeen to ConfigSchema

In `src/main/config-store.ts`:
- Add `firstRunSeen: z.boolean().default(false)` to ConfigSchema
- The type AppConfig will automatically include firstRunSeen
</task>

<task id="2" type="auto">
### Add IpcChannels for Phase 6

In `src/shared/ipc-channels.ts`:
- Add `ConfigMarkFirstRunSeen: 'config:mark-first-run-seen'`
- Add `LauncherVerifySpoof: 'launcher:verify-spoof'`
- Add `LauncherOpenVerificationUrls: 'launcher:open-verification-urls'`
- Add `ConfigGetFirstRunSeen: 'config:get-first-run-seen'`
</task>

<task id="3" type="auto">
### Write pid.txt sentinel on instance launch

In `src/launcher/index.ts`:
- After `mkdtemp`, write `pid.txt` containing `String(process.pid)` into the userDataDir
- Import `writeFileSync` from `node:fs` (sync is fine here — before Playwright launch)
</task>

<task id="4" type="auto">
### Sweep orphaned profile dirs on startup

In `src/main/index.ts`:
- Import `readdirSync, rmSync, readFileSync, existsSync` from `node:fs` and `tmpdir` from `node:os`
- Create a `sweepOrphanedProfiles()` function that:
  1. Reads tmpdir() entries
  2. Filters for names matching /^anticlicker-profile-/
  3. For each, tries to read `pid.txt` inside it
  4. Calls `process.kill(pid, 0)` — if it throws ESRCH, the process is dead → delete dir
  5. If pid.txt missing, treat as orphaned → delete
  6. Wraps everything in try-catch (best-effort, never throw)
- Call `sweepOrphanedProfiles()` inside `app.whenReady()` before `createWindow()`
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- ConfigSchema has `firstRunSeen` field
- IpcChannels has the 4 new entries
- launcher writes `pid.txt` on launch
- sweepOrphanedProfiles function exists and is called on startup
</verification>

<done_criteria>
TypeScript compiles; pid.txt sentinel writing + orphan sweep wired.
</done_criteria>
