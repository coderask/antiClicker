---
phase: 06-verification-package
plan: 03
type: execute
wave: 1
depends_on: [06-02]
files_modified:
  - src/preload/index.ts
  - src/renderer/src/App.tsx
  - src/renderer/src/Sidebar.tsx
  - src/renderer/src/ScopeOverlay.tsx
autonomous: true
requirements:
  - VRF-01
  - VRF-02
  - VRF-03
tags:
  - renderer
  - ux
  - overlay
  - verify-button
---

<objective>
1. Extend contextBridge preload with new methods: verifySpoof, openVerificationUrls, markFirstRunSeen, getFirstRunSeen.
2. Add per-instance "Verify" button to Sidebar with inline result display.
3. Add ScopeOverlay.tsx — first-run modal, full-viewport, single dismiss button.
4. Wire chromium-missing error toast (already handled by launchError state in App.tsx).
5. Wire firstRunSeen check on App mount; show overlay until dismissed.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/06-verification-package/06-RESEARCH.md
</context>

<tasks>

<task id="1" type="auto">
### Extend preload contextBridge

In `src/preload/index.ts`:
- Add `verifySpoof(id: string): Promise<{reported: {lat: number, lng: number}, expected: {lat: number, lng: number}, match: boolean}>` → invoke `LauncherVerifySpoof`
- Add `openVerificationUrls(id: string): Promise<void>` → invoke `LauncherOpenVerificationUrls`
- Add `markFirstRunSeen(): Promise<void>` → invoke `ConfigMarkFirstRunSeen`
- Add `getFirstRunSeen(): Promise<boolean>` → invoke `ConfigGetFirstRunSeen`
</task>

<task id="2" type="auto">
### Add ScopeOverlay component

Create `src/renderer/src/ScopeOverlay.tsx`:
- Props: `{ onDismiss: () => void }`
- Full-viewport overlay (position: fixed, inset 0, z-index 1000)
- Semi-transparent dark background (#0008)
- Centered white card with:
  - Title: "AntiClicker — Coordinate Spoof Only"
  - Body: "This tool overrides your browser's geolocation to the map pin coordinates. Your IP address, timezone, and browser language are unchanged — only the GPS coordinates reported to websites are affected."
  - Button "Got it" (data-testid="scope-overlay-dismiss")
- Container data-testid="scope-overlay"
</task>

<task id="3" type="auto">
### Wire ScopeOverlay into App.tsx

In `src/renderer/src/App.tsx`:
- Add state: `const [showOverlay, setShowOverlay] = useState(false)`
- In useEffect on mount, call `window.api.getFirstRunSeen()`, if false → `setShowOverlay(true)`
- Dismiss handler: call `window.api.markFirstRunSeen()` → `setShowOverlay(false)`
- Render `{showOverlay && <ScopeOverlay onDismiss={handleDismissOverlay} />}` at top of return
- Import ScopeOverlay
</task>

<task id="4" type="auto">
### Add Verify button to Sidebar

In `src/renderer/src/Sidebar.tsx`:
- Add props: `onVerify: (id: string) => void`, `onOpenVerificationUrls: (id: string) => void`, `verifyResults: Map<string, {match: boolean, reported: {lat: number, lng: number}}>`
- In each instance row, add:
  - "Verify" button (`data-testid={`verify-${inst.id}`}`) that calls `onVerify(inst.id)`
  - Small "Links" button (`data-testid={`verify-urls-${inst.id}`}`) that calls `onOpenVerificationUrls(inst.id)`
  - If `verifyResults.has(inst.id)`: render result inline below coords:
    - Green checkmark "match" if result.match, red X "mismatch" otherwise
    - Show reported lat/lng values
    - `data-testid={`verify-result-${inst.id}`}`
</task>

<task id="5" type="auto">
### Wire verify handlers in App.tsx

In `src/renderer/src/App.tsx`:
- Add state: `const [verifyResults, setVerifyResults] = useState<Map<string, {match: boolean, reported: {lat: number, lng: number}}>>(new Map())`
- Add `handleVerify` async: calls `window.api.verifySpoof(id)`, stores result in Map
- Add `handleOpenVerificationUrls`: calls `window.api.openVerificationUrls(id)`
- Pass `onVerify={handleVerify}`, `onOpenVerificationUrls={handleOpenVerificationUrls}`, `verifyResults={verifyResults}` to Sidebar
- The existing `launchError` + `data-testid="launch-error"` already shows any chromium-missing error
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0
- ScopeOverlay.tsx exists with data-testid="scope-overlay" and data-testid="scope-overlay-dismiss"
- Sidebar has verify button with data-testid="verify-{id}"
- Preload exposes verifySpoof, openVerificationUrls, markFirstRunSeen, getFirstRunSeen
</verification>

<done_criteria>
TypeScript compiles; all UI components rendered; testids correct.
</done_criteria>
