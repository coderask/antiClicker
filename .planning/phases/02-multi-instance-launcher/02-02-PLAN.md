---
phase: 02-multi-instance-launcher
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/launcher/registry.test.ts
  - tests/launcher/fixture.ts
  - vitest.config.ts
autonomous: true
requirements:
  - LCH-04
  - LCH-05
tags:
  - launcher
  - unit-test
  - fixture
---

<objective>
Add pure-logic unit tests for the Registry class (no browser required) and extract the localhost
HTTP fixture server into a shared helper that integration tests can reuse.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/02-multi-instance-launcher/02-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/tests/cli/geolocation.spec.ts
@/Users/aarnavk/Documents/Github/antiClicker/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry unit tests</name>
  <files>tests/launcher/registry.test.ts, vitest.config.ts</files>
  <action>
    Create `tests/launcher/registry.test.ts` with vitest unit tests (no browser, no playwright)
    for the Registry class. Tests must cover:
    - add() + list() returns correct Instance shape
    - get() returns entry; get() on missing ID returns undefined
    - getOrThrow() throws on missing ID
    - remove() removes entry; subsequent list() excludes it
    - list() never exposes the context property
    - size() reflects current count

    Update `vitest.config.ts` to add `tests/launcher/**/*.test.ts` to the include list.
  </action>
  <done>npm run test:unit passes including the new registry tests (6+ tests, no browser launched).</done>
</task>

<task type="auto">
  <name>Task 2: Fixture server helper</name>
  <files>tests/launcher/fixture.ts</files>
  <action>
    Extract the inline fixture server from `tests/cli/geolocation.spec.ts` into
    `tests/launcher/fixture.ts`. Export:
    - `GEO_FIXTURE_HTML` — the HTML that calls getCurrentPosition and writes to #result
    - `COOKIE_FIXTURE_HTML` — HTML that sets document.cookie and reads it back (for isolation test)
    - `startGeoFixtureServer(): Promise<{ url: string; close: () => Promise<void> }>` — starts
      the node:http server on 127.0.0.1:0, serves GEO_FIXTURE_HTML on all paths except
      /cookie (serves COOKIE_FIXTURE_HTML), returns base URL and close function
    - `waitForGeoResult(page): Promise<string>` — waits for #result to leave 'pending'
  </action>
  <done>File exists and exports startGeoFixtureServer; it starts/stops without error when called from Node.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` exits 0 (registry tests pass)
- `tests/launcher/fixture.ts` exports startGeoFixtureServer
</verification>
