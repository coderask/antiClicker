---
phase: 02-multi-instance-launcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/coords-schema.ts
  - src/launcher/registry.ts
  - src/launcher/instance.ts
  - src/launcher/index.ts
autonomous: true
requirements:
  - LCH-04
  - LCH-05
  - LCH-06
  - LCH-07
  - LCH-08
tags:
  - launcher
  - multi-instance
  - playwright
  - module
---

<objective>
Implement the `src/launcher/` module: a reusable factory that launches N concurrent isolated
Chromium instances via Playwright, each with its own ephemeral profile dir, geolocation override,
and live setGeo support. Also extract the shared Zod coordinate schema into `src/shared/coords-schema.ts`.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/02-multi-instance-launcher/02-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/scripts/cli-prototype.ts
@/Users/aarnavk/Documents/Github/antiClicker/src/shared/types.ts
@/Users/aarnavk/Documents/Github/antiClicker/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared coords schema</name>
  <files>src/shared/coords-schema.ts</files>
  <action>
    Create `src/shared/coords-schema.ts` that exports `ZodCoordsSchema` (a zod object with
    latitude [-90,90], longitude [-180,180], accuracy optional positive number) and the inferred
    `Coords` type. This is the single source of truth for coordinate validation used by the
    launcher and future IPC layer.
  </action>
  <done>File exists; `ZodCoordsSchema.parse({ latitude: 0, longitude: 0 })` succeeds; out-of-range values throw ZodError.</done>
</task>

<task type="auto">
  <name>Task 2: Registry</name>
  <files>src/launcher/registry.ts</files>
  <action>
    Create `src/launcher/registry.ts` exporting `InstanceEntry` type (id, context, userDataDir,
    coords) and `Registry` class with: `add(entry)`, `get(id): InstanceEntry | undefined`,
    `getOrThrow(id): InstanceEntry`, `remove(id)`, `list(): Instance[]`, `size(): number`.
    `list()` maps entries to the public `Instance` shape (no context exposed). Pure logic —
    no Playwright imports needed.
  </action>
  <done>Registry add/get/remove/list work correctly; list() never exposes context.</done>
</task>

<task type="auto">
  <name>Task 3: Launcher module</name>
  <files>src/launcher/instance.ts, src/launcher/index.ts</files>
  <action>
    Create `src/launcher/instance.ts` with `launchInstance(opts, registry, emit)` that:
    1. Validates coords via ZodCoordsSchema
    2. Generates a short ID (crypto.randomUUID().slice(0,8))
    3. Creates userDataDir via mkdtemp(join(tmpdir(), 'anticlicker-profile-'))
    4. Calls chromium.launchPersistentContext with headless:false, geolocation, permissions
    5. Registers entry in registry
    6. Attaches context.on('close') handler that removes from registry, best-effort rm -rf userDataDir, emits 'instance-closed'
    7. Returns Instance (public shape)

    Create `src/launcher/index.ts` that exports all public types and `createLauncher()` factory
    returning a Launcher object with launch/setGeo/close/closeAll/list/on methods. setGeo
    validates coords, calls context.setGeolocation, updates registry coords. close calls
    context.close() (the 'close' event handler does the rest). closeAll closes all in parallel
    via Promise.allSettled. on() registers event listeners for 'instance-closed'.
  </action>
  <done>createLauncher() is callable; launch() returns an Instance with id/coords/userDataDir; setGeo/close/closeAll/list/on are all present on the returned object.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0 (all three new files type-clean)
- `src/launcher/index.ts` exports: Coords, LaunchOptions, InstanceId, Instance, Launcher, createLauncher
- `src/shared/coords-schema.ts` exports: ZodCoordsSchema, Coords
</verification>
