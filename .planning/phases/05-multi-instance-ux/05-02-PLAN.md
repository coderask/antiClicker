---
phase: 05-multi-instance-ux
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/renderer/src/Sidebar.tsx
autonomous: true
requirements:
  - MIX-01
  - MIX-02
  - MIX-04
tags:
  - sidebar
  - instances
  - focus
  - close
---

<objective>
Create src/renderer/src/Sidebar.tsx — a vertical panel listing running instances with:
short ID, colored dot, coords, close button, click-to-focus. Active instance highlighted.
Includes the RecentPins section at the bottom.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/05-multi-instance-ux/05-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar.tsx component</name>
  <files>src/renderer/src/Sidebar.tsx</files>
  <action>
    Create src/renderer/src/Sidebar.tsx.

    Props interface:
    ```ts
    export interface SidebarInstance {
      id: string;
      coords: { latitude: number; longitude: number };
      color: string;
    }

    export interface SidebarProps {
      instances: Map<string, SidebarInstance>;
      activeId: string | null;
      onFocus: (id: string) => void;
      onClose: (id: string) => void;
    }
    ```

    Layout:
    - Outer container: 280px wide, full height, dark background (#1a1a2e),
      flex-column, border-left: 1px solid #333, overflow: hidden
    - Section header: "Running Instances" label (font 11px, color #888, padding 8px 12px)
    - Instance list: flex: 1, overflow-y: auto (scrollable if many instances)
    - Each instance row:
      - data-testid: activeId === id ? "instance-row-active" : "instance-row"
      - onClick: onFocus(id)
      - style: display flex, gap 8, align-items center, padding 8px 12px,
               cursor pointer, background: activeId === id ? rgba(255,255,255,0.08) : transparent
               border-left: 3px solid (activeId === id ? color : transparent)
      - Contents:
        * Colored dot: 10px circle, background: color
        * ID short: first 8 chars of id, font-size 11px monospace, flex: 1
        * Coords: lat.toFixed(4) + "," + lng.toFixed(4), font-size 10px, color #aaa
        * Close button: "×", data-testid: `close-${id}`, onClick stops propagation + calls onClose(id)
          style: color #888, background none, border none, cursor pointer, font-size 14px, padding 0 4px
    - Empty state: if instances.size === 0, show "No active instances" in muted text

    data-testid attributes (required):
    - Container: data-testid="sidebar"
    - Active row: data-testid="instance-row-active"
    - Non-active row: data-testid="instance-row"
    - Close button: data-testid={`close-${id}`}
  </action>
  <done>
    Sidebar.tsx created; exported SidebarInstance + SidebarProps types; all data-testid attrs present;
    npm run typecheck passes.
  </done>
</task>

</tasks>

<verification>
  npm run typecheck — exits 0.
</verification>

<success_criteria>
  1. Sidebar.tsx exports default Sidebar component.
  2. Active row gets data-testid="instance-row-active".
  3. Close button gets data-testid={`close-${id}`}.
  4. Clicking row fires onFocus(id).
  5. Clicking close fires onClose(id) without also triggering onFocus.
  6. npm run typecheck passes.
</success_criteria>
