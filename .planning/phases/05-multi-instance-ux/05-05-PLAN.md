---
phase: 05-multi-instance-ux
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - tests/unit/ringBuffer.test.ts
  - tests/renderer/Sidebar.test.tsx
  - tests/e2e/multi-instance-flow.spec.ts
autonomous: true
requirements:
  - MIX-01
  - MIX-02
  - MIX-03
  - MIX-04
  - MIX-05
tags:
  - tests
  - unit
  - component
  - e2e
---

<objective>
Add tests for Phase 5 components:
1. Unit tests for the ring-buffer utility
2. Component tests for the Sidebar (happy-dom)
3. E2E test for the full multi-instance flow
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/05-multi-instance-ux/05-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/tests/renderer/CoordInput.test.tsx
@/Users/aarnavk/Documents/Github/antiClicker/tests/e2e/map-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ring-buffer unit tests</name>
  <files>tests/unit/ringBuffer.test.ts</files>
  <action>
    Create tests/unit/ringBuffer.test.ts. Test the pushBounded function:
    1. Returns array with item appended (basic)
    2. Caps at max (inserts 12 items, max=10, expect length 10)
    3. Removes oldest items (front), keeps newest
    4. max=1 returns single-element array (always latest)
    5. max=0 returns empty array
    6. Does not mutate the input array (immutability check)
    7. Works with object items (not just primitives)

    Use vitest describe/it/expect.
  </action>
  <done>
    ringBuffer.test.ts created with 7 tests; all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar component tests</name>
  <files>tests/renderer/Sidebar.test.tsx</files>
  <action>
    Create tests/renderer/Sidebar.test.tsx using @testing-library/react with happy-dom.

    Test cases:
    1. "renders empty state when no instances" — instances = new Map(); assert "No active instances" visible
    2. "renders one row per instance" — 3 instances, expect 3 rows (data-testid="instance-row" or "instance-row-active")
    3. "active row has data-testid instance-row-active" — instances[1] is activeId; assert that row has correct testid
    4. "clicking a row fires onFocus with id" — click second row, expect onFocus called with that id
    5. "clicking close button fires onClose without firing onFocus" — click close-{id}, expect onClose called once, onFocus NOT called
    6. "close button stops propagation" — use vi.fn() for both callbacks, assert onClose called, onFocus not called

    Use Map constructor: new Map([['id1', { id: 'id1', coords: { latitude: 35, longitude: 139 }, color: '#e74c3c' }], ...])
  </action>
  <done>
    Sidebar.test.tsx created with 6 tests; all pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Multi-instance E2E test</name>
  <files>tests/e2e/multi-instance-flow.spec.ts</files>
  <action>
    Create tests/e2e/multi-instance-flow.spec.ts.

    This test covers all 5 Phase 5 success criteria:

    Test flow:
    1. Launch the Electron app (built output)
    2. Wait for app ready (ping = "pong")
    3. Wait for canvas visible

    Instance 1:
    4. Click canvas at position (400, 300) — drops draft pin
    5. Assert pin-coords not empty
    6. Click launch-here-button
    7. Wait for live-instances = "1"
    8. Assert sidebar has 1 instance row (data-testid="instance-row" or "instance-row-active")

    Instance 2:
    9. Click canvas at position (700, 300) — drops new draft pin (different coords)
    10. Click launch-here-button
    11. Wait for live-instances = "2"
    12. Assert sidebar has 2 instance rows total

    SC#2 — Focus/pan:
    13. Get the first instance row and click it
    14. Assert it becomes data-testid="instance-row-active"

    SC#4 — Close:
    15. Find the close button for the second instance row
    16. Click it
    17. Wait for live-instances = "1"
    18. Assert sidebar has 1 instance row

    SC#5 — Recent pins:
    19. Assert data-testid="recent-pins" is visible
    20. Assert at least 1 data-testid="recent-pin-row" exists (from the two launches)

    Notes:
    - test.setTimeout(180_000) — 3 Chrome windows launched
    - Use try/finally { await app.close() }
    - The exact close button testid is not known until render — use the pattern:
      const rows = window.locator('[data-testid="instance-row"], [data-testid="instance-row-active"]')
      const firstRowId = await rows.first().getAttribute('data-instance-id') || ''
      — or simply get all close buttons and click the second one (for the second instance)
    - For SC#3 (drag → setGeo), this is tested via the existing setGeo IPC path;
      full drag-then-verify-coords would require reading navigator.geolocation in the launched
      Chrome which is a Phase 6 concern. Document this as a known test scope boundary.
  </action>
  <done>
    multi-instance-flow.spec.ts created; covers SC#1 (2 instances + 2 sidebar rows),
    SC#2 (click row → active), SC#4 (close → 1 row), SC#5 (recent pins visible).
  </done>
</task>

</tasks>

<verification>
  npm run typecheck — exits 0.
  npm run test:unit — ringBuffer.test.ts passes (7 tests).
  npm run test:unit (renderer) — Sidebar.test.tsx passes (6 tests).
  npm run test:e2e — multi-instance-flow.spec.ts passes.
</verification>

<success_criteria>
  1. ringBuffer unit tests: 7 passing.
  2. Sidebar component tests: 6 passing.
  3. E2E multi-instance flow: proves SC#1, SC#2, SC#4, SC#5 from Phase 5 success criteria.
  4. All prior tests remain green.
</success_criteria>
