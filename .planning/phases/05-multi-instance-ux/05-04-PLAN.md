---
phase: 05-multi-instance-ux
plan: 04
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/renderer/src/RecentPins.tsx
  - src/renderer/src/utils/ringBuffer.ts
autonomous: true
requirements:
  - MIX-05
  - MIX-06
tags:
  - recent-pins
  - ring-buffer
---

<objective>
Create the ring-buffer utility and RecentPins component. Ring buffer caps the list at 10 items
(session-only, no disk persistence). Clicking a recent pin populates the CoordInput and flies
the map there without auto-launching.
</objective>

<context>
@/Users/aarnavk/Documents/Github/antiClicker/.planning/phases/05-multi-instance-ux/05-RESEARCH.md
@/Users/aarnavk/Documents/Github/antiClicker/src/renderer/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ringBuffer utility</name>
  <files>src/renderer/src/utils/ringBuffer.ts</files>
  <action>
    Create src/renderer/src/utils/ringBuffer.ts:

    ```ts
    /**
     * Returns a new array with `item` appended, capped at `max` items.
     * Oldest items (from the front) are removed when the cap is exceeded.
     */
    export function pushBounded<T>(arr: readonly T[], item: T, max: number): T[] {
      if (max <= 0) return [];
      return [...arr, item].slice(-max);
    }
    ```

    No default export — named export only.
  </action>
  <done>
    ringBuffer.ts created with pushBounded export; npm run typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecentPins component</name>
  <files>src/renderer/src/RecentPins.tsx</files>
  <action>
    Create src/renderer/src/RecentPins.tsx:

    Props interface:
    ```ts
    export interface RecentPinsProps {
      pins: Array<{ latitude: number; longitude: number }>;
      onSelect: (coords: { latitude: number; longitude: number }) => void;
    }
    ```

    Layout:
    - Section header: "Recent Pins" (font 11px, color #888, padding 8px 12px)
    - List: max-height ~160px, overflow-y: auto
    - Each pin row: button element, flex row, data-testid="recent-pin-row"
      - Content: lat.toFixed(4) + ", " + lng.toFixed(4)
      - onClick: onSelect(coords)
      - style: width 100%, text-align left, background none/hover, border none, color #ccc,
               padding 4px 12px, font-size 11px, cursor pointer, font-family monospace
    - Show most recent first (reverse order): pins.slice().reverse()
    - Empty state: "No recent pins" in muted text (data-testid="recent-pins-empty")
    - Container data-testid="recent-pins"
  </action>
  <done>
    RecentPins.tsx created; data-testid="recent-pins" present; clicking row fires onSelect;
    npm run typecheck passes.
  </done>
</task>

</tasks>

<verification>
  npm run typecheck — exits 0.
</verification>

<success_criteria>
  1. pushBounded caps at max, removes oldest items from front.
  2. RecentPins renders pins in reverse order (newest first).
  3. Clicking a pin row calls onSelect with correct coords.
  4. Empty state shown when pins array is empty.
  5. npm run typecheck passes.
</success_criteria>
